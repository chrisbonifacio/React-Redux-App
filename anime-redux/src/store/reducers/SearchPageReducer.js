const initialStateForSearchPage = {
    term: '',
    results: []
}

export function searchPageReducer(state = initialStateForSearchPage, action) {
    switch(action.type) {
        default: 
            return state
    }
}